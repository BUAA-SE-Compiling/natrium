name: R0VM Test

on:
  push:
  pull_request:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: ChangeCwd
      run: cd crates/r0vm/
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
